// BattleBot.JS

/*
Handles fight logic and updates user stats depending on the outcome of the fight
*/

// Handles commands from other bots
let handleCommand = (command) => {
    // Parse Command:
    // Determine if user wants to begin a fight instance or fight a zone boss
}

// Fights the boss of a specific zone
let fightBoss = (zone) => {
    // Fight Boss Here
}

// Fights in a specific zone for numFights times
let beginInstance = (zone, numFights) => {
    // Begin instance here
}

// Determines if the user's stats would win in one random fight against a mob in the given zone
let fightMob = (zone, userStats) => {

}
